## Dockerizing the Spring Boot Application with MySql using Docker Networks

Creating the SpringBoot Application with port 8080 and create the docker-compose.yml file for connecting the two containers.


Use 
```zh
docker-compose up
```

